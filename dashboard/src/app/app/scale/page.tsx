// Scale floor — re-exports /app/operations content at /app/scale for
// canonical tier-named URL. `/app/operations` redirects here via
// next.config.ts. Physical folder move deferred.
export { default } from "../operations/page";
